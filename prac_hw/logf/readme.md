
# Реализация натурального логарифма

Требуется имплементировать float функции натурального логарифма ln с точностью <3.5 ulp в режиме округления `TO NEAREST` на С. Диапазон аргумента 2-126 S x < 2127. Решение должно иметь вид:
```C
float logf (float x) {
    return res;
}
```
Решение должно быть корректным на всем указанном диапазоне аргументов. Использовать функции сторонних библиотек, например gnu libc, нельзя.

## Вычисление дисперсии $DX$

При использовании чисел с плавающей точкой выбранный алгоритм критически влияет на точность результата.

## 1. "Быстрый" алгоритм
Данный алгоритм основан на определении:
```math
DX = \frac{1}{n} \sum_{i=1}^{n} x_i^2 - \left( \frac{1}{n} \sum_{i=1}^{n} x_i \right)^2
```
При близких значениях уменьшаемого и вычитаемого происходит потеря значимости, вплоть до отрицательности результата.

## 2. Двухпроходный алгоритм
В основе алгоритма лежит поочередное вычисления среднего $M$ и сумма квадратов отклонений $DX$:
```math
M = \frac{1}{n} \sum_{i=1}^n x_i \\
DX = \frac{1}{n} \sum_{i=1}^{n} (x_i - M)^2
```
Данный алгоритм, по сравнению с предыдущим, более устойчив, но требует два прохода по данным.

## 3. Однопроходной алгоритм
Алгоритм позволяет вычислять дисперсию за один проход с высокой точностью. В основе работы лежит обновление среднего $M_n$ и суммы квадратов $DX_n$ для каждого нового значения $x_n$:

```math
M_{n+1} = M_{n} + \frac{x_{n+1} - M_{n}}{n + 1} \\
```
```math
DX_{n+1} = DX_{n} + \frac{(x_{n+1} - M_{n})(x_{n+1} - M_{n+1}) - DX_n}{n + 1}
```

## Результаты

Для получение результатов исследования следует выполнить компиляцию и запуск программы:
```shell
make && ./float
```

Результаты ошибок для трёх выборок для `float` и `double` по 1000 чисел:
- mean = 1, stdvar = 1
- mean = 10, stdvar = 0.1
- mean = 100, stdvar = 0.01

| Параметры выборки | Тип | Алгоритм | Результат (DX) | Относ. ошибка |
| :--- | :--- | :--- | :--- | :--- |
| **Mean=1, σ=1** | Double | Fast | 9.630187e-01 | 3.7% |
| (Theo DX: 1.0) | Double | Two-Pass | 9.630187e-01 | 3.7% |
| | Double | One-Pass | 9.630187e-01 | 3.7% |
| | Float | Fast | 9.630175e-01 | 3.7% |
| | Float | Two-Pass | 9.630188e-01 | 3.7% |
| | Float | One-Pass | 9.630186e-01 | 3.7% |
| --- | --- | --- | --- | --- |
| **Mean=10, σ=0.1** | Double | Fast | 9.630187e-03 | 3.7% |
| (Theo DX: 0.01) | Double | Two-Pass | 9.630187e-03 | 3.7% |
| | Double | One-Pass | 9.630187e-03 | 3.7% |
| | Float | Fast | 9.605408e-03 | 3.9% |
| | Float | Two-Pass | 9.630183e-03 | 3.7% |
| | Float | One-Pass | 9.630128e-03 | 3.7% |
| --- | --- | --- | --- | --- |
| **Mean=100, σ=0.01** | Double | Fast | 9.630187e-05 | 3.7% |
| (Theo DX: 0.0001) | Double | Two-Pass | 9.630187e-05 | 3.7% |
| | Double | One-Pass | 9.630187e-05 | 3.7% |
| | **Float** | **Fast** | **1.757812e-02** | **17478.1%** |
| | Float | Two-Pass | 9.630469e-05 | 3.7% |
| | Float | One-Pass | 9.629759e-05 | 3.7% |


###  Ошибка
Ошибка считалась относительно порядка отклонения. Для `double` разницы в методах нет, остаётся небольшая статистическая ошибка в 3.7%. Для `float` видно проявление критической потери точности для *быстрого* алгоритма - ошибка в 17000%. Также *быстрый* алгоритм проигрывает остальным алгоритмам в точности и без критической потери.