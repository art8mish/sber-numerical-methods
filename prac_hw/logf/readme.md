
# Реализация ln

Требуется имплементировать float функции натурального логарифма ln с точностью <3.5 ulp в режиме округления `TO NEAREST` на С. Диапазон аргумента $2^{-126} \leq x < 2^{127}$ (положительные нормализованные числа; для субнормалей реализация также определена). Решение должно иметь вид:

```C
float logf (float x) {
    return res;
}
```

Решение должно быть корректным на всем указанном диапазоне аргументов. Использовать функции сторонних библиотек, например gnu libc, нельзя.

## Описание решения

Для $x > 0$ в формате IEEE-754 single разбиваем $x = 2^e \cdot r$, где $r \in [1, 2)$ - мантисса после нормализации, $e$ - целый порядок. Для субнормального $x$ сначала сдвигаем биты влево, пока не появится ведущая единица, поправляя $e$ (стандартная нормализация). Подствляя $x$: $ln x = e \cdot ln 2 + ln r$. Константа $ln 2$ задана как `double` с полной точностью для промежуточных вычислений.

Для вычисления $ln r$ используется подстановка $u = \frac{(r − 1)}{(r + 1)}$, то есть $ln r = 2 \cdot atanh(u)$. Чтобы уменьшить ошибку аппроксимации, перед полиномом выполняется дополнительная редукция диапазона: при $r > \sqrt{2}$ берётся $r \leftarrow r/2$ и $e \leftarrow e+1$. После этого мантисса лежит ближе к 1, что заметно повышает точность на точках около 1.

Скалярные выражения считаются в `double`, итог приводится к `float` (округление к ближайшему по правилам IEEE). Так достигается запас по сравнению с требованием **< 3.5 ulp** без вызова `libm`.

## Особые случаи

При $x \leq 0$ (включая $−0$): для нуля возвращается $-\infty$ с `errno = ERANGE`, `FE_DIVBYZERO`. Для отрицательного значения возвращается NaN с `errno = EDOM`, `FE_INVALID` (как при вызове `log` из libm). Значения $+\infty$ и NaN возвращаются как есть, без установки `errno`.

## Сборка

Команда `make` в каталоге `logf` собирает объектный файл `logf.o` (исходник - C++ для удобства, точка входа объявлена как `extern "C" float logf(float)` для связки с кодом на C).

## Тестирование

Реализация представлена в файле [test_logf.cpp](test_logf.cpp). Эталон - `std::log(double)` (сборка с `-fno-builtin-log` и `-fno-builtin-logf`). Проверяются `errno` и маска `FE` как у libm на особых аргументах ($+0$, $−0$, $−1$, $-\infty$, $+\infty$, NaN и отрицательные субнормали). На интервалах по битам положительных конечных чисел проверяются $ULP \leq 4$ и `errno == 0` после `logf`. Проводятся явные проверки точек и плотный диапазон вокруг 1.0, чтобы не пропускать локальные ULP‑пики. 

Сборка и запуск:
```shell
make test
```

## Оптимизация

В качестве оптимизации представлена векторизованная версия [logf_avx2.cpp](logf_avx2.cpp). Функция `__m256 logf8_avx2(__m256 x)` обрабатывает 8 `float` за вызов с использованием `AVX2 + FMA`. Математика как в [logf.cpp](logf.cpp) в области нормализованныx положительныx конечных чисел без особых случаев.

Тесты реализации в [test_logf_avx2.cpp](test_logf_avx2.cpp). 
Сборка и запуск:
```shell
make test_avx2
```

## Производительность

Для измерения производительности реализован бенчмарк [perftest.cpp](perftest.cpp).

Замер длительности производится в тактах TSC через `rdtsc` / `rdtscp` с сериализацией `lfence`. Особенности измерений взяты с задания [perftest](../perftest/readme.md). В качестве метрики используется **CPE** (cycles per float).

Сборка и запуск:
```shell
make perf
```

### Результаты эксперимента

Параметры замеров: `ITERS = 5'000'000`, `WARMUP_RUNS = 4`, `MEASURE_RUNS = 13`, `SCALAR_LANES = 8`, `VECTOR_LANES = 4`.

```text
Performance test: libm logf vs custom logf vs vector logf8_avx2
iters=5000000, warmup_runs=4, measure_runs=13, scalar_lanes=8, vector_lanes=4

Libm logf latency (CPE):
	median_cpe  = 30.444058
	cv_percent  = 0.471027%

Libm logf throughput (CPE):
	median_cpe  = 8.267031
	cv_percent  = 4.272907%

Custom logf latency (CPE):
	median_cpe  = 84.945340
	cv_percent  = 0.141391%

Custom logf throughput (CPE):
	median_cpe  = 23.441980
	cv_percent  = 4.722811%

Vector logf8_avx2 latency (CPE):
	median_cpe  = 9.930106
	cv_percent  = 0.911411%

Vector logf8_avx2 throughput (CPE):
	median_cpe  = 4.701558
	cv_percent  = 4.437303%

Speedup vs vector (median CPE, lower is better; ratio = slower/faster):
	latency:    libm=30.444058  custom=84.945340  vector=9.930106
	            libm/vector=3.065834  custom/vector=8.554324
	throughput: libm=8.267031  custom=23.441980  vector=4.701558
	            libm/vector=1.758360  custom/vector=4.986003
```

По **throughput** медиана `logf8_avx2` около **4.70 CPE**, в **1.76** раза быстрее `std::log` и в **4.99** раза быстрее скалярного `logf`. По **latency** векторная реализация **9.93 CPE**, в **3.07** раза быстрее `std::log` из libm и в **8.55** раза быстрее пользовательского скаляра. 

Скалярный `logf` ожидаемо медленнее `std::log` в обоих режимах: длиннее полином, дополнительная работа с `errno` и FE, ветвления для особых случаев. 

Стоит отметить, что сравнение по производительности с `logf8_avx2` производится только на нормализованных положительных конечных числах.

## Вычислительное окружение

- Ядро: `Linux 6.8.0-40-generic`
- Компилятор: `g++ (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0`
- CPU: `AMD Ryzen 5 5600H with Radeon Graphics`, 6 ядер / 12 потоков, x86_64