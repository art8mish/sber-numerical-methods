
# Потеря точности float[32|64]

Требуется исследовать проблему потери точности, реализовав три метода для вычисления оценки дисперсии в float[32|64] с использованием как минимум 3 выборок:
- 1000 чисел из нормального распределения (среднее = 1, среднеквадратическое отклонение = 1)
- 1000 чисел из нормального распределения (среднее = 10, среднеквадратическое отклонение = 0.1)
- 1000 чисел из нормального распределения (среднее = 100, среднеквадратическое отклонение = 0.01)

## Вычисление дисперсии $DX$

При использовании чисел с плавающей точкой выбранный алгоритм критически влияет на точность результата.

## 1. "Быстрый" алгоритм
Данный алгоритм основан на определении:
```math
DX = \frac{1}{n} \sum_{i=1}^{n} x_i^2 - \left( \frac{1}{n} \sum_{i=1}^{n} x_i \right)^2
```
При близких значениях уменьшаемого и вычитаемого происходит потеря значимости, вплоть до отрицательности результата.

## 2. Двухпроходный алгоритм
В основе алгоритма лежит поочередное вычисления среднего $M$ и сумма квадратов отклонений $DX$:
```math
M = \frac{1}{n} \sum_{i=1}^n x_i \\
DX = \frac{1}{n} \sum_{i=1}^{n} (x_i - M)^2
```
Данный алгоритм, по сравнению с предыдущим, более устойчив, но требует два прохода по данным.

## 3. Однопроходной алгоритм
Алгоритм позволяет вычислять дисперсию за один проход с высокой точностью. В основе работы лежит обновление среднего $M_n$ и суммы квадратов $DX_n$ для каждого нового значения $x_n$:

```math
M_{n+1} = M_{n} + \frac{x_{n+1} - M_{n}}{n + 1} \\
```
```math
DX_{n+1} = DX_{n} + \frac{(x_{n+1} - M_{n})(x_{n+1} - M_{n+1}) - DX_n}{n + 1}
```

## Результаты

Для получение результатов исследования следует выполнить компиляцию и запуск программы:
```shell
make && ./float
```

Результаты ошибок для трёх выборок для `float` и `double` по 1000 чисел:
- mean = 1, stdvar = 1
- mean = 10, stdvar = 0.1
- mean = 100, stdvar = 0.01

| Параметры выборки | Тип | Алгоритм | Результат (DX) | Относ. ошибка |
| :--- | :--- | :--- | :--- | :--- |
| **Mean=1, σ=1** | Double | Fast | 9.630187314414e-01 | 3.7% |
| (Theo DX: 1.0) | Double | Two-Pass | 9.630187314414e-01 | 3.7% |
| | Double | One-Pass | 9.630187314414e-01 | 3.7% |
| | Float | Fast | 1.046357870102e+00 | 4.6% |
| | Float | Two-Pass | 1.046358466148e+00 | 4.6% |
| | Float | One-Pass | 1.046357393265e+00 | 4.6% |
| --- | --- | --- | --- | --- |
| **Mean=10, σ=0.1** | Double | Fast | 9.630187314428e-03 | 3.7% |
| (Theo DX: 0.01) | Double | Two-Pass | 9.630187314414e-03 | 3.7% |
| | Double | One-Pass | 9.630187314414e-03 | 3.7% |
| | Float | Fast | 1.049041748047e-02 | 4.9% |
| | Float | Two-Pass | 1.046358607709e-02 | 4.6% |
| | Float | One-Pass | 1.046356279403e-02 | 4.6% |
| --- | --- | --- | --- | --- |
| **Mean=100, σ=0.01** | Double | Fast | 9.630187014409e-05 | 3.7% |
| (Theo DX: 0.0001) | Double | Two-Pass | 9.630187314414e-05 | 3.7% |
| | Double | One-Pass | 9.630187314417e-05 | 3.7% |
| | **Float** | **Fast** | **2.929687500000e-03** | **2829.7%** |
| | Float | Two-Pass | 1.046339602908e-04 | 4.6% |
| | Float | One-Pass | 1.046288962243e-04 | 4.6% |


###  Ошибка
Ошибка считалась относительно порядка отклонения. Для `double` статистическая ошибка остаётся около 3.7%; при полной точности вывода на наборе Mean=100, σ=0.01 у *быстрого* метода видна небольшая численная разница относительно двухпроходного и однопроходного. Для `float` — критическая потеря точности у *быстрого* алгоритма при Mean=100, σ=0.01 (ошибка ~2800%). *Быстрый* алгоритм на `float` проигрывает остальным и без этого крайнего случая.